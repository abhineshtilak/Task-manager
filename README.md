# Task Manager

A pure HTML, CSS and JavaScript daily task manager. Tasks are stored in browser localStorage, so no backend or cloud database is required.

## Features
- Add tasks with priority
- Mark tasks complete
- Filter all/pending/completed
- Progress statistics
- Delete and clear completed tasks
- Persistent browser localStorage
- Responsive layout
